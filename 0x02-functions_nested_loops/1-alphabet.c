#include "main.h"
/**
 *print_alphabet_x10 - program entry point
 *
 *Return: zero if succesful
 */
void print_alphabet_x10(void)
{
	i = 'a';
	for (i; i <= 'z'; i++)
	{
		_putchar(i);
	}
	putchar('\n');
	return (0);
}
